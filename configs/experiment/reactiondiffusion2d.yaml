domain:
  x: [0.0, 1.0]
  y: [0.0, 1.0]
  t: [0.0, 1.0]

D: 1.0                 # diffusion coefficient
r: 0.0                 # reaction rate
lambda: auto           # if "auto": lambda = D*2*pi^2 - r so residual == 0
in_features: 3         # (x, y, t)
out_features: 1
time_dependent: true
